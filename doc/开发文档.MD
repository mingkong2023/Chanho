
# Hono.js
npm install hono

# Drizzle ORM 及其 MySQL 适配器 (drizzle-orm with mysql2)
npm install drizzle-orm mysql2

# 开发依赖：用于类型生成和迁移
npm install -D drizzle-kit

# 可选：TypeScript 支持
npm install -D typescript tsx @types/node